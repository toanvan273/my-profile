const withImages = require("next-images");
module.exports = {
    reactStrictMode: true,
    i18n: {
        locales: ["vi", "en"],
        defaultLocale: "vi"
    },
    optimizeFonts: true
    // withImages()
};
